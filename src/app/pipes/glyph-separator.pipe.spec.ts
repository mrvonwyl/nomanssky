import { GlyphSeparatorPipe } from './glyph-separator.pipe';

describe('GlyphSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new GlyphSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
